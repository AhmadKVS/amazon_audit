version: 1
frontend:
  phases:
    preBuild:
      commands:
        - git submodule update --init --recursive
        - cd frontend && npm ci
    build:
      commands:
        - cd frontend && npm run build
  artifacts:
    baseDirectory: frontend/.next
    files:
      - '**/*'
  cache:
    paths:
      - frontend/.npm/**/*
